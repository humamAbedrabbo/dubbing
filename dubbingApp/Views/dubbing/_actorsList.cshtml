@model IEnumerable <dubbingModel.dubbingAppointment>
@{ int id = 0; }

<div class="container-fluid" style="padding:0px">
    <ul style="padding-left:15px">
        @foreach (var actor in Model.Select(b => new { b.voiceActorIntno, b.actorName }).Distinct())
        {
            id++;
            string menuId = "D" + id.ToString();
            string actorName = '"' + actor.actorName + '"';
            <li>
                <a class="actorLbl" id="@menuId" href="javascript:onActorSelectClick(@actor.voiceActorIntno, @actorName, '@menuId')">@actor.actorName</a>
            </li>
        }
    </ul>
</div>


